require 'test_helper'

class EventsControllerTest < ActionController::TestCase
  test ".create creates a new Event with Event.create_with_journey" do

    #journey = Journey.find_or_create_open_journey_for_user(user)
    #Event.create(journey: journey, slug: params[:slug])

  end
end
