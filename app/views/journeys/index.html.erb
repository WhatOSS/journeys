<h1>Journeys</h1>

<ul class="journeys">
  <% @journeys.each do |journey| %>
    <li>
      <span class="time">
        <%= journey.created_at.strftime("%Y-%m-%d %H:%M") %>
      </span>
      <span class="user"><%= journey.user.ip %></span>
    </li>
  <% end %>
</ul>
